{
  "buildCommand": "cd frontendWeb && npm install && npm run build",
  "outputDirectory": "frontendWeb/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
